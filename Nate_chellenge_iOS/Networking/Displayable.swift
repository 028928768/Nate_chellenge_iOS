

protocol Displayable {
    var idLabel: String { get }
    var createdLabel: String { get }
    var updatedLabel: String { get }
    var titleLabel: String { get }
    var imagesUrl: [String] { get }
    var urlLabel: String { get }
    var merchantLabel: String { get }
   

    
}
